jdk:
  - openjdk11

before_install:
  - chmod +x gradlew
  - git update-index --chmod=+x gradlew

install:
  - ./gradlew clean -Pgroup=com.github.anil098421
  - ./gradlew build publishToMavenLocal -Pgroup=com.github.anil098421 